# Proton Mail SMTP submission settings
SMTP_SERVER=smtp.protonmail.ch
SMTP_PORT=587
# Use the address paired with your Proton SMTP token.
SMTP_USERNAME=
# This is the SMTP token password, not your normal Proton password.
SMTP_PASSWORD=
FROM_EMAIL=
FROM_NAME=Luke Henderson
TO_EMAIL=